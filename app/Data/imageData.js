export const secondRow = Array.from({ length: 10 }, (_, i) => ({
  src: `/poza${i + 1}.webp`,
}));

export const thirdRow = Array.from({ length: 10 }, (_, i) => ({
  src: `/poza${i + 11}.webp`,
}));
export const firstRow = Array.from({ length: 10 }, (_, i) => ({
  src: `/poza${i + 21}.webp`,
}));
