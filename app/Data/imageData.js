export const secondRow = Array.from({ length: 10 }, (_, i) => ({
  src: `/Radu/${i + 1}.webp`,
}));

export const thirdRow = Array.from({ length: 19 }, (_, i) => ({
  src: `/Ed/${i + 1}.webp`,
}));
export const firstRow = Array.from({ length: 21 }, (_, i) => ({
  src: `/Mihaela/${i + 1}.webp`,
}));
